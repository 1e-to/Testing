name: LALALA

on:
  push:
    branches:
    tags:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Echo
        run: echo "BUILD"
      - name: Ref
        run: echo ${{ github.ref }}

  upload:
    needs: build
    if: ${{ github.ref }} == 'refs/heads/main' || ${{ github.ref }} == 'refs/heads/release*'
    runs-on: ubuntu-latest

    steps:
      - name: Echo
        run: echo "UPLOAD"